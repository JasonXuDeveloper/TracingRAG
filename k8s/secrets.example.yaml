# Example secrets file - DO NOT commit actual secrets to Git
# Create actual secrets using: kubectl create secret generic tracingrag-secrets --from-literal=...
apiVersion: v1
kind: Secret
metadata:
  name: tracingrag-secrets
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres:5432/tracingrag"
  redis-url: "redis://redis:6379/0"
  neo4j-username: "neo4j"
  neo4j-password: "changeme"
  openrouter-api-key: "your-openrouter-api-key"
  jwt-secret-key: "your-jwt-secret-key-change-in-production"
